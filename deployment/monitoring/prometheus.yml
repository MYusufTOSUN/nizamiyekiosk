global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'bilimfest_orchestrator'
    metrics_path: /api/v1/metrics
    static_configs:
      - targets: ['localhost:8000']
        labels:
          service: bilimfest
          phase: production
